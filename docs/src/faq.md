Does scoop work on Windows?
Yes! Since scoop-uv is built on top of uv, it has excellent cross-platform support. You can use it via:

PowerShell (Recommended)

Command Prompt (cmd.exe)

Git Bash or WSL Note: Make sure to follow the Windows-specific shell integration steps in the Installation guide.
